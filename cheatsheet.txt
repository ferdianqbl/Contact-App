# application Middleware

    :: memanggilnya dengan

        app.use((req, res, next) => {
            // lakukan sesuatu

            next(); // agar middleware berikutnya bisa dijalankan
        });

# Built in

    :: app.use(express.static('public')); // untuk mengakses img, css, dll selain code, yg disimpan di public